---
id: PDOC-002
revision: 1
depends_on: [../README.md]
---
# Architecture overview

Placeholder content. `depends_on` above intentionally uses a legacy relative
path instead of a stable ID, so `docsystem migration-report` and `docsystem
readiness` have a real resolvable migration to report against this fixture.
